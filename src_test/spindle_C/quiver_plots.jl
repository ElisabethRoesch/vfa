using PyPlot
gr()
PyPlot.quiver(coord_list[1,:], coord_list[2,:], grad_list[1,:],grad_list[1,:] )

show(p)
coord_list[][1]

len = 1331


coord_list

size(coord_list)[1]

coord_list[:,2]

s = zeros(5)

s[length(s)]

coord_list[1,:]
p
